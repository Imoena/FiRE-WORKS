  <h1>本当に退会しますか？</h1>
    <div class="messages">
        <h3>退会すると、会員登録情報や<br>
        これまでの投稿が閲覧できなくなります。<br>
        退会する場合は、「退会する」をクリックしてください。
        </h3>
    </div>

    <%= link_to "　退会しない", user_path(current_user.id), class: "glyphicon glyphicon-home btn btn-primary" %>
    <%= link_to "　退会する　", user_path(current_user.id), class: "destroy-user",method: :delete, data: {confirm: "本当に退会してもよろしいですか？"}%>