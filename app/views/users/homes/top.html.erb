<!------ 運営おすすめイベントスライド ------>
<div class="top-slide-group">
    <div class="top-slide"><img class="top-image" src="assets/show1.jpg"></div>
    <div class="top-slide"><img class="top-image" src="assets/show2.jpg"></div>
    <div class="top-slide"><img class="top-image" src="assets/show3.jpg"></div>
</div>

<script type="text/javascript">
 $(function() {
    $('.top-slide-group').slick({
        dots: true,
        autoplay: true,
        centerMode: true,
        centerPadding: '18%',
        autoplaySpeed: 3000,
        speed:1500,
    });
});
</script>

<%# @topic_event.each do |event| %>
<%#= event.festival %>
<%#= attachment_image_tag event, :event_image, format: 'jpeg', fallback: "no-image.jpg", size:'300x250'%>
<%# end %>

<!------ 都道府県別ランキング ------>
<% if user_signed_in? %>
<h4 class="top-index"><%= @user.prefecture %>のおすすめ</h4>
  <div class="row">
    <%= render partial: 'users/posts/index',locals: { posts: @user_prefecture_posts } %>
  </div>
  <% else %>
  <% end %>


<!------ お気に入り数ランキング ------>
<h4 class="top-index">お気に入りランキング</h4>
　<div class="row">
    <%= render partial: 'users/posts/index',locals: { posts: @posts } %>
  </div>

<!------ カレンダー ------>
<h4 class="top-index">カレンダーから探す</h4>
  <div id="calendar-anc">
     <div class="row-xs-12">
          <div class="calendar col-xs-12">
            <%= month_calendar events: @events do |date, events| %>
      	      <%= date.day %>
      	      <% events.each do |event| %>
      		    <div>
      		      <%= link_to event.festival, event_path(event),class: "calendar-link-style", data: {"turbolinks" => false} %>
      		    </div>
                <% end %>
            <% end %>
          </div>
      </div>
  </div>

<!----- クリッカブルマップ ------>
<h4 class="top-index" id="map-box" >都道府県から探す</h4>
<div class="row">
<%= render partial: 'japan_map' %>
</div>

