<h4>会員一覧</h4>
<table class="table">
        <thead>
          <tr>
            <th>会員ID</th>
            <th>氏名</th>
            <th>プロフィール画像</th>
            <th>メールアドレス</th>
            <th>登録日</th>
          </tr>
        </thead>

        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.id %></td>
              <td><%= link_to user.name, admins_user_path(user) , class: "entire-link-style"%></td>
              <td><%= attachment_image_tag user, :profile_image, format: 'jpeg', size:'40x40', fallback: "no-image.jpg" %></td>
              <td><%= user.email %></td>
              <td><%= l user.created_at, format: :short %></td>
            </tr>
            <% end %>
        </tbody>
      </table>